{
  "name": "wave-check",
  "version": "0.0.1",
  "main": "index.js",
  "repository": "https://github.com/lshadler/wave-check.git",
  "author": "wtait1, lshadler",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "@auto-it/conventional-commits": "^9.40.5",
    "@auto-it/git-tag": "^9.40.5",
    "@auto-it/released": "^9.40.5",
    "auto": "^9.40.5",
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install": "lerna bootstrap",
    "clean": "rm -rf node_modules",
    "build": "lerna run build --stream --parallel",
    "start": "lerna run start --stream --parallel",
    "test": "lerna run start --stream --parallel"
  }
}